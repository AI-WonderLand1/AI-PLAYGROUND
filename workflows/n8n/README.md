# n8n Workflow Examples

This directory contains exported n8n workflow JSON files used as examples, references, and importable workflow assets for AI-PLAYGROUND.

The exported files are intentionally kept with their original filenames and content. They are not part of the Vite/Express runtime build.

## Examples
See `examples/` for the preserved workflow exports.
