playground.dreammakerhub.website, ai-playground.dreammakerhub.website {
    encode zstd gzip

    handle /api/* {
        reverse_proxy localhost:3001
    }

    handle {
        root * /var/www/html
        try_files {path} /index.html
        file_server
    }
}
